# sa
분류 -> 범주형 변수를 자동 수치형으로 변환해주는 csvkit (인공지능을 위한)

- 인코딩 -> csv 언어를 인지해서 인코딩해주는 기능
https://jvvp.tistory.com/1022

- 한글꺠짐 방지

csvkit과 chatGPT API 를 연결해서 csv 파일을 넣으면 파일에 대한 기본 정보를 출력해주는...
chatGPT 에다 데이터 넣고 데이터 정보를 알려주기
csv 데이터를 문제로 읽어와서 기본 통계량 출력해주기

데이터베이스 '뷰' 개념 => csvkit 명령어를 사용하여 뷰를 만들고 원하는 데이터 셋을 보여주는 것.

어떤..기존의 프로그램? 과 결합하여 csv 파일을 그 프로그램에 접목..

깃허브 REST API+ CSVKIT => csv 파일을 바로 깃허브에 올려준다.


미세먼지 데이터를 입력하여 예측해주는 csvkit
- 데이터 처리 -> 월평균 대기오염도, 월별변화량, 계절 특성으로 입력 데이터 만들기, 출력은 월평균 대기오염도 -> 회귀모델, 예측수행 -> 모델 평가
- 계절 특성 : 봄,여름,가을,겨울 각 계절에 대한 월평균 대기오염도 수치 계산
(원핫 인코딩 봄 [1,0,0,0])


결측치 처리 csvkit 자동 데이터 클리닝,-> 결측치, 이상치, 중복 데이터 식별 자동 처리 기능
결측치 식별 : NaN, 빈값 확인
결측치 처리 : 대체값, 평균이나 중간값, 행or 열 제거
이상치처리: 평균이나 중간값으로 대체


#dh
- csvkit은 데이터 처리에 초점을 맞추고 있으니 시각화 기능을 추가
  시각화 라이브러리와 통합하여 차트 생성 등의 기능..

- 일관된 방식으로 데이터 처리하는 스크립트 작성하는 기능(작업의 자동화/반복적인 작업 처리)

